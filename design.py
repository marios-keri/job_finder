# NEEDS
# list of job portals
# search engine query structure
# requests
# query builder

# take a job title as input from user                       # take a description job
# build a query compatible with usr input + engine          # post the jobs to all portals
    # make the request                                      # done
    # applay to all job results
    # done
